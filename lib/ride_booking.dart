import 'package:flutter/material.dart';

class RideBooking extends StatelessWidget {
  // const RideBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("rideBooking page")));
  }
}
